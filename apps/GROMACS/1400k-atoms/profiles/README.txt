Cirrus/ = directory containing VTune text output from Cirrus on a single node
ARCHER/ = directory containing CrayPat text output from ARCHER on a single node
run/ = directory containing the scripts used to generate the VTune and CrayPat profiles
