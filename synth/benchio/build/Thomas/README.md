# benchio Build: MMM Thomas

Environment set correctly on login

Copy ``Makefille.thomas`` into ``shared-file/source`` directory

Build:

```bash
cd shared-file/source
make
```

Binary is called ``benchio``.
