# benchio Build: CSD3-Skylake

Environment set correctly on login

Copy ``Makefile.csd3skylake`` into ``shared-file/source`` directory

Build:

```bash
cd shared-file/source
make -f Makefile.csd3skylake
```

Binary is called ``benchio``.
